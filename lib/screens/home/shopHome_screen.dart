import 'package:flutter/material.dart';

class shopHome_screen extends StatelessWidget {
  const shopHome_screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('shopHome screen')),
    );
  }
}